# ==============================================================================
# Doplo Prometheus Configuration
# ==============================================================================
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "/etc/prometheus/alert_rules.yml"

scrape_configs:
  # 1. Doplo Fastify REST API Scraper
  - job_name: "doplo-api"
    scrape_interval: 10s
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8080"]

  # 2. Doplo BullMQ Worker Scraper (Internal Container Network)
  - job_name: "doplo-worker"
    scrape_interval: 10s
    metrics_path: "/metrics"
    static_configs:
      - targets: ["worker:9090"]

  # 3. Node Exporter & Textfile Collector (Host & Backup Status Metrics)
  - job_name: "node-exporter"
    scrape_interval: 15s
    static_configs:
      - targets: ["node-exporter:9100"]
